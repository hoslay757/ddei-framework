//初始化外部配置
const config = {
  SELECTOR: {
    //选择器边框
    border: {
      width: 1,
      color: "red",
      selected: {
        width: 1,
        color: "blue",
      },
    },
  },
};

export default config